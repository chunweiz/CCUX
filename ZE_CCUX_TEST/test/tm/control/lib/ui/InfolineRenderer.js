/*global sap*/

sap.ui.define(
    [],

    function () {
        'use strict';

        var ControlRenderer = {};

        ControlRenderer.render = function (oRm, oControl) {

        };

        return ControlRenderer;
    },

    true
);
